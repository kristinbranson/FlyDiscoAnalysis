function test_transfero_FlyDiscoPipeline_wrapper_on_experiment_list(folder_path_from_experiment_index, ...
                                                                    cluster_billing_account_name, ...
                                                                    user_name_for_configuration_purposes, ...
                                                                    do_use_bqueue, ...
                                                                    do_actually_submit_jobs, ...
                                                                    submit_host_name, ...
                                                                    varargin)
         
    %test_transfero_FlyDiscoPipeline_wrapper_on_experiment_list
    %   Runs the FlyDisco pipeline on a set of experiment folders.
    %
    %   This is a simple pass-through wrapper for
    %   run_transfero_FlyDiscoPipeline_wrapper_on_experiment_list().  It is
    %   maintained only in the interests of backwards compatibity.
    %
    %   See the documentation of
    %   run_transfero_FlyDiscoPipeline_wrapper_on_experiment_list() for more
    %   information.

    % Process arguments                                
    if ~exist('do_use_bqueue', 'var') || isempty(do_use_bqueue) ,
        do_use_bqueue = true ;
    end
    if ~exist('do_actually_submit_jobs', 'var') || isempty(do_actually_submit_jobs) ,
        do_actually_submit_jobs = true ;
    end
    if ~exist('ssh_host_name', 'var') || isempty(submit_host_name) ,
        submit_host_name = '' ;
    end

    run_transfero_FlyDiscoPipeline_wrapper_on_experiment_list(folder_path_from_experiment_index, ...
                                                              cluster_billing_account_name, ...
                                                              user_name_for_configuration_purposes, ...
                                                              do_use_bqueue, ...
                                                              do_actually_submit_jobs, ...
                                                              submit_host_name, ...
                                                              varargin{:}) ;
end
